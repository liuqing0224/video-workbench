# 文章流程浏览器自动化回归

日期：2026-09-17。方式：用户指定 browser-control，Ego Lite TaskSpace 108「文章流程自动化回归」，p1；实际页面 http://127.0.0.1:8794/。空间已完成并关闭测试页。

测试项目：`cc8504ce5bab43f1ae0e97cdfaae54d3`（QA｜文章入口浏览器回归）。正式项目 `9294e11eb53341e6bd0436ba97dd7acf` 只读检查，不提交确认或修改。

| 场景 | 结果 | 可观察证据 |
|---|---|---|
| 读取文章分析 | 通过 | 编辑区包含推荐选题“畅销正装”及原文证据边界 |
| 读取口播脚本 | 通过 | SCRIPT.md 编辑区包含 SEG01 |
| 策划确认页面 | 通过 | 内容设计待确认、真实任务摘要、16项产物；确认按钮可用，未点击 |
| 新建测试项目 | 通过 | 页面显示指定项目名称及教学分支 |
| 空文章校验 | 条件通过 | 使用可读副本后显示“文章内容为空或不是有效文本”；空文件仍留在素材列表 |
| 正常文章上传与排队 | 通过 | 显示“文章已导入”，内容任务出现；上传控件 disabled=true |
| 取消任务 | 通过 | 页面变为“已取消”，最近运行显示“用户取消”，重试入口出现 |
| 文档编辑与保存 | 通过 | 测试 BRIEF 增加回归标记；点击保存，切换文件再打开，标记仍在 |
| 快速项目切换 | 本次通过 | Menza→QA→Menza 后等待内容加载，标题保持 Menza；未观察到自动切换 |
| 后续交接读取 | 通过 | PRODUCTION-PLAN.md 能读取，包含 Qwen 试音交接 |

## 发现与限制

Ego 在本轮不能读取 Documents 下测试文件字节：直接上传表现为 Failed to fetch，Ego Node 读取同一文件报 EPERM，服务器没有收到上传请求。使用 browser-control 自带 stageVerifiedMedia 在本地创建同名、SHA-256 一致的私有副本后，通过原页面真实 input 上传成功。没有改浏览器权限或清除登录状态。

测试文件：`article-browser-regression/测试文章.md`（289 bytes，SHA-256 `8ce5c6c20296d9d15caef086c366c46215e7ee44d1030169f7e85008748c8038`）和空文章。该问题属于本次浏览器文件读取环境限制，不能宣称直接 Documents 上传已通过；平台错误信息仍可改进。

本轮测试了文章入口及策划交接 UI；正常测试任务随后被主动取消，未重复执行完整模型策划。正式 Menza 策划完成来自上一轮真实运行，本轮重新验证其可读取和待确认状态。未测本轮新的视频渲染、配音、试听、完整播放或发布。没有把自动化测试当作用户内容批准。

测试中因保存按钮编辑后显示“保存 *”调整了定位；一次等待条件超时后先检查页面，没有重复创建项目。测试项目及素材保留用于复核。
