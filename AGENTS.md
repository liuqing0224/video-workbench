# 视频制作平台项目规则

采用一套公共制作流程、两个内容分支、一组可复用模板。公共内容规则见《视频制作总工作流》，运行接口见 docs/运行手册.md。

- `.agents/skills/video-orchestrator` 组织阶段；营销与教学 Skills 只负责各自内容方法。声音由 video-qwen-narration，交付由 video-delivery-qa 处理。画面先读取现有 hyperframes 入口，按需加载其领域 Skills。
- 案例驱动设计是新片与视觉重做的默认要求：先看已有案例证据，写 REFERENCES.md 镜头映射，验证关键小样实际画面，再做整片。自动模式不能跳过；技术 QA 不代替设计验收。具体规则以《视频制作总工作流》“案例如何进入设计”为准，用户明确选择优先，局部修复沿用有效设计。
- 事实、推断、示意、待验证项分别记录，案例画面研究不能当作音轨或效果验证。
- 项目 documents 中 Markdown/JSON 是内容依据；SQLite 是运行、确认与事件依据；STATUS.md 是导出视图，不能反向写数据库状态。
- 稳定编号不复用。每次产物记录输入哈希和工具版本。修改后检查相关声音、字幕、镜头、练习、答案及最终 QA；保留历史运行副本。
- 内容、小样、正式声音、完整预览的确认绑定具体版本；已确认且依赖未变的决定继续有效。Agent 不能伪造人工确认。
- 模型只在运行副本修改制作文件。平台库、凭据、父目录不属于任务写入范围。工具通过参数数组执行，不接受网页任意 shell 命令。
- 未生成的文件、未执行的检查、未开展的学习测试不能标记完成。最终哈希来自实际文件。
- 公开发布和付费投放不在默认制作权限内。
- 平台首版为单 Worker、单活动制作任务，不启动多 Agent。
- 源码和详细状态留在本项目；Obsidian 只维护轻量注册卡。

开发验证：`.venv/bin/python -m pytest`；`npm --prefix apps/web run build`。不要用正式用户项目运行破坏性测试，使用 pytest 临时目录。
- 普通用户以文章为输入，不要求用户导入脚本或制作 JSON。article-plan 阶段负责提炼受众问题、推荐单一选题、标题开场、口播、证据、案例映射和分镜草案；事实不擅自扩写为产品能力。后续任务沿用这些文件，小样与实际配音之后再锁定时间。

## 视频视觉方向与确认

新片和视觉重做必须记录参考帧/时间点、颜色角色、具体借鉴与不借鉴项；没有品牌约束时，不继承上一项目配色。候选配色明确标记为设计推导，不能冒充案例采样值。先完成实际动态小样，再由用户选择方向；自动化技术检查不能替代审美确认。Marketing不固定深色，Teaching不固定浅色。

平台通过 documents/VISUAL-PLAN.json 关联候选设计、小样和证据，以SQLite visual_reviews记录主体及指纹；文件中“已确认”文字不构成批准。新建UI项目默认要求视觉确认，历史项目可通过新增VISUAL-PLAN.json进入视觉修订。纯视觉修改保留有效声音定时，但使预览、渲染与QA重新检查。调用方必须如实标记actor；浏览器Agent不得冒用user记录自己的判断。规则与数据示例见 docs/视觉方向确认.md。
