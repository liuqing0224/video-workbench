# 文件交接约定

这是文档与 JSON 约定，不是已实现的 API、自动调度器或 HyperFrames 官方配置格式。制作时由 Codex 将数据转入实际工程，并检查两者一致性。

## 从模板开始

从 [模板说明](templates/README.md) 复制 common，再合并营销或教学文件。工程目录由本次项目指定；不要覆盖模板或示例。填写 BRIEF 和 STATUS 后按 [总工作流](视频制作总工作流.md) 执行。

缺失资料记录为待补充；JSON 用 null 表示未知，用空数组表示尚未建立记录。未知不等于零，不等于成功。示例中的 estimated 时间不允许直接标记 locked。

## 文件职责与关联

| 文件 | 交接内容 | 谁消费 |
|---|---|---|
| SCRIPT.md | segment_id、屏幕文字、正式写法、发音写法、claim_ids | 声音生成与镜头设计 |
| STORYBOARD.md | shot_id、segment_id、信息目标、状态变化、最低阅读时间 | 时间设计与画面制作 |
| claims-map.json | claim_id、主张、事实/推断/示意、来源、asset_ids、核验状态、公开边界 | 脚本审查与 QA |
| assets-manifest.json | asset_id、路径、来源、版本、角色、可用状态、使用限制 | 工程与交付 |
| components-map.json | 组件用途/实现状态、镜头与素材对应 | HyperFrames 制作 |
| voice-config.json | Qwen 版本、音色、参数、发音表、按 segment_id 的生成记录 | 声音修订与复现 |
| timing.json | fps、总时长、镜头、音轨和字幕时间 | 场景编排、字幕与同步验收 |
| steps.json | 教学步骤、目标、动作、预期结果、排错、练习、shot_ids | 教学制作与复做 |
| delivery.json | 输入版本、工具版本、输出文件及哈希、QA 与效果状态 | 最终交付 |

编号在内容修改后尽量保持稳定；只有新增内容分配新编号，删除后不将旧编号用于另一内容。项目内不能重复编号；所有引用必须存在。

文件路径相对于视频工程根目录。可用素材路径必须真实存在；计划素材可以为 null。不要把未来音频路径伪装成已有文件。服务配置只保存引用或不含凭据的地址，不保存密钥、令牌或带认证参数的 URL。

## 时间字段

时间单位统一为秒；start_s 是全片绝对起点，duration_s 是持续时长。镜头终点为两者之和。正式制作按帧率对齐时间；字幕不得超过全片，音频引用必须能找到资产。

镜头顺序和重叠必须符合分镜；转场重叠可存在，但须在分镜写明原因。不能仅通过“各段相加等于总时长”判断有重叠的视频正确。

音轨条目格式示意：`{"asset_id":"A02","start_s":0,"duration_s":6,"source_start_s":0,"segment_ids":["SEG01"]}`。其中 source_start_s 是素材内裁切起点；时长来自实际音频。示例值不代表已生成的音频。

字幕条目格式示意：`{"caption_id":"CAP01","segment_id":"SEG01","start_s":0,"duration_s":2,"text":"显示文本"}`。逐词高亮需要可靠的词级时间，不能从整句字幕平均分配后声称精确对齐。

timing 的状态由 estimated 转为 locked，必须有对应素材、正式声音确认（无旁白则记录不适用）、字幕校正及可读性检查。变更后先标为需要重新确认，重新生成受影响内容。

## 声音与无旁白

voice-config 的 requests 将 segment_id、发音文本、音频路径和版本关联；保留原始输出与正式音频的关系，裁切与后处理记录在状态/素材记录中。本地 Qwen 未验证前 model_version、voice 等保持 null。

无旁白示例见 [数据示例](templates/data-examples/timing-no-narration.json)：不需要 voice-config 或转录字幕。有 BGM 时仍要记录音轨并做混音验收；“无旁白”不自动意味着“无声音”。

## 交付与哈希绑定

delivery.outputs 每条记录实际 path、variant（如 horizontal-v1）、sha256；delivery.qa_records 每条记录 path、output_path、output_sha256、status。每个变体独立对应 QA，未执行不填写通过。

哈希由最终文件计算，不能手填虚构值。QA 报告中也写同一个 MP4 路径和哈希，关联关键帧与音频/编码分析。工具版本和输入修订在交付前填实。

示意（不是已存在文件）：`{"path":"qa/horizontal-v1.md","output_path":"exports/horizontal-v1.mp4","output_sha256":null,"status":"not_run"}`。

最终文件变化后旧 QA 留作历史记录，不覆盖成新版本证据。效果验证可保持 not_done；营销转化和教学学习效果必须使用真实反馈。
